# 🚀 راهنمای استقرار سایت در هاست cPanel

وب‌سایت دبیرستان شهید علی‌اکبر نجفی — کرمانشاه
Node.js + TypeScript + Tailwind CSS — بدون هیچ وابستگی runtime (فقط Node)

---

## ✅ پیش‌نیازها

| مورد | نیاز |
|---|---|
| هاست | cPanel با قابلیت **Setup Node.js App** (روی CloudLinux با اسم Node.js Selector) |
| Node.js | نسخه **۲۰ LTS** یا بالاتر (سازگار با ES2022) |
| فایل ZIP | همین فایل `najafi-school-site.zip` که دانلود کردید |

> اگر هاست شما Node.js Selector ندارد، از پشتیبانی هاست بخواهید آن را فعال کند.
> اگر اصلاً Node روی هاست موجود نیست، می‌توانید از یک VPS کوچک یا هاست ابری ایرانی که Node دارد استفاده کنید.

---

## 📦 محتویات ZIP

```
najafi-school-site/
├── src/                  ← کد TypeScript (منبع)
├── dist/                 ← خروجی کامپایل‌شده (آماده اجرا — از قبل ساخته شده)
├── public/               ← فایل‌های استاتیک (CSS، favicon) — robots و sitemap داینامیک ساخته می‌شوند
│   └── assets/app.css    ← خروجی Tailwind (از قبل ساخته شده)
├── styles/input.css      ← ورودی Tailwind
├── data/db.json          ← پایگاه‌داده (تنظیمات، اخبار، کلاس‌ها، دانش‌آموزان و...)
├── uploads/              ← فایل‌های آپلودشده (PDF، عکس‌ها)
├── package.json / tsconfig.json / tailwind.config.js
└── README.md / DEPLOY-CPANEL.md
```

> ⚠️ پوشه `node_modules` داخل ZIP نیست — بعد از آپلود با دستور نصب ساخته می‌شود (فقط برای بیلد لازم است؛ خود سایت بدون آن هم اجرا می‌شود چون dist از قبل آماده است).

---

## 🥇 روش ۱ — Setup Node.js App (پیشنهادی)

### قدم ۱: آپلود و استخراج
1. وارد cPanel شوید → **File Manager**.
2. به پوشه‌ای که می‌خواهید سایت آنجا باشد بروید:
   - برای دامنه اصلی: `public_html`
   - برای ساب‌دامین: پوشه همان ساب‌دامین (مثلاً `school.example.com`)
3. دکمه **Upload** → فایل ZIP را آپلود کنید.
4. روی فایل ZIP راست‌کلیک → **Extract** → همه فایل‌ها همان‌جا باز می‌شوند.
   (مطمئن شوید فایل‌ها مستقیم داخل پوشه هدف هستند، نه داخل یک زیرپوشه اضافی)

### قدم ۲: ساخت اپلیکیشن Node
1. در cPanel بخش **Software** → **Setup Node.js App** (یا Node.js Selector).
2. دکمه **Create Application** را بزنید و مقادیر زیر را وارد کنید:

| فیلد | مقدار |
|---|---|
| Node.js version | **20.x** (یا بالاتر) |
| Application mode | **Production** |
| Application root | پوشه‌ای که فایل‌ها را Extract کردید (مثلاً `/home/user/public_html`) |
| Application URL | دامنه یا ساب‌دامین موردنظر |
| Application startup file | **`dist/server.js`** |

3. **Create** را بزنید.

### قدم ۳: نصب پکیج‌ها و (در صورت نیاز) بیلد
1. داخل همان صفحه، دکمه **Run NPM Install** را بزنید (فقط devDependency ها نصب می‌شوند).
2. از آنجایی که `dist` از قبل داخل ZIP ساخته شده، **بیلد لازم نیست** — اما اگر بعداً کد را تغییر دادید:
   - دکمه **Run JS script** را بزنید و در کادر command بنویسید: `npm run build:all`

### قدم ۴: تنظیم آدرس سایت (⚠️ خیلی مهم برای گوگل)
1. در همان صفحه Setup Node.js App، بخش **Environment variables** → **Add Variable**.
2. متغیر زیر را اضافه کنید (به‌جای `example.ir` دامنه واقعی خودتان را با `https` بنویسید):

| Name | Value |
|---|---|
| `BASE_URL` | `https://example.ir` |

3. **Save** را بزنید.

> این آدرس در نقشه سایت (`/sitemap.xml`)، تگ canonical و داده‌های ساخت‌یافته گوگل استفاده می‌شود. اگر ست نکنید، گوگل آدرس اشتباه ایندکس می‌کند و رتبه نمی‌گیرید!

### قدم ۵: اجرا
- دکمه **Restart** (یا Start) را بزنید.
- چند ثانیه صبر کنید، سپس آدرس سایت را در مرورگر باز کنید. ✅

### قدم ۶: ورود به پنل مدیریت
- آدرس: `https://دامنه-شما/admin/login`
- نام کاربری: `admin`
- رمز: `Najafi@1404`

> 🔐 **فوراً رمز را عوض کنید:** پنل مدیریت → «تنظیمات سایت» → تغییر رمز.

---

## 🥈 روش ۲ — ترمینال / SSH

اگر دسترسی Terminal یا SSH دارید:

```bash
cd ~/public_html                     # یا پوشه هدف
unzip najafi-school-site.zip
npm install --no-audit --no-fund     # فقط برای بیلد لازم است
npm run build:all                    # فقط در صورت تغییر کد
node dist/server.js                  # اجرای موقت (برای تست)
```

> برای اجرای دائمی، باز هم استفاده از **Setup Node.js App** بهتر است تا برنامه همیشه روشن بماند و بعد از ری‌استارت هاست خودکار بالا بیاید.

---

## 📌 نکات مهم

- **پورت:** هیچ تنظیمی لازم نیست! برنامه از متغیر `PORT` که cPanel می‌دهد استفاده می‌کند (`src/config.ts`). اگر دستی روی سیستم خودتان اجرا کنید روی `3000` بالا می‌آید.
- **داده‌ها:** تمام محتوا (اخبار، اطلاعیه‌ها، کلاس‌ها، دانش‌آموزان، غیبت‌ها، تنظیمات) در فایل `data/db.json` ذخیره می‌شود. هر چند وقت یک‌بار از پنل مدیریت → «پشتیبان‌گیری» → **بکاپ کامل سایت (ZIP)** بگیرید (شامل دیتابیس و همه فایل‌های `uploads/`).
- **مجوز نوشتن:** پوشه‌های `data/` و `uploads/` باید قابل نوشتن باشند (به‌صورت پیش‌فرض در cPanel هستند: پوشه‌ها 755، فایل‌ها 644).
- **بعد از هر تغییر کد:** `npm run build:all` را اجرا کنید (CSS و TypeScript دوباره ساخته می‌شوند) و اپلیکیشن را Restart کنید.
- **اگر تغییرات را ندیدید:** مرورگر را Hard Refresh کنید (Ctrl+Shift+R) و لاگ Passenger را از بخش Setup Node.js App بررسی کنید.

---

## 🔍 ثبت سایت در گوگل (قدم‌های اول شدن در جست‌وجو)

سایت از نظر فنی برای سئو آماده است (تایتل و توضیحات فارسی، canonical، نقشه سایت، robots، داده ساخت‌یافته School/NewsArticle/FAQ). بعد از بالا آمدن روی دامنه واقعی، این کارها را **شما** باید انجام دهید:

1. **سرچ کنسول گوگل:** وارد [Google Search Console](https://search.google.com/search-console) شوید → سایت را با روش Domain یا URL prefix ثبت و مالکیت را تأیید کنید → در بخش Sitemaps آدرس `https://دامنه-شما/sitemap.xml` را ثبت کنید.
2. **درخواست ایندکس:** در همان کنسول، آدرس صفحه اصلی را Inspect کنید و **Request Indexing** بزنید.
3. **گواهی SSL:** از cPanel بخش SSL/TLS، گواهی رایگان (AutoSSL/Let's Encrypt) را فعال کنید تا آدرس با `https` باز شود — بدون https گوگل رتبه خوب نمی‌دهد.
4. **ثبت در نقشه‌ها:** مدرسه را با **همان نام و آدرس و تلفن دقیق سایت** در Google Maps (Business Profile)، نشان و بلد ثبت کنید.
5. **لینک‌سازی:** از سایت آموزش‌وپرورش ناحیه/استان، کانال‌های مدرسه (ایتا/تلگرام/اینستاگرام) و آپارات به سایت لینک بدهید.
6. **محتوای تازه:** هفته‌ای حداقل یک خبر با عنوان حاوی «نجفی» و «کرمانشاه» منتشر کنید و برای هر خبر عکس واقعی با کاور بگذارید.

> ⏳ اسم «دبیرستان نجفی کرمانشاه» رقیب جدی ندارد؛ معمولاً **۱ تا ۴ هفته بعد از ایندکس**، با جست‌وجوی همین عبارت رتبه اول می‌شوید.

---

## 🛠 رفع اشکال سریع

| مشکل | راه‌حل |
|---|---|
| خطای 503 / app fails to start | Startup file را `dist/server.js` بگذارید و Node نسخه ۲۰ بگذارید؛ بعد Restart |
| Passenger spawn failed | مطمئن شوید پوشه `dist/` وجود دارد (اگر نیست: `npm install` سپس `npm run build`) |
| سایت بالا می‌آید ولی صفحه سفید است | Hard Refresh + بررسی Passenger log |
| آپلود فایل/عکس خطا می‌دهد | مجوز نوشتن پوشه `uploads/` را چک کنید (755 یا 775) |
| بعد از آپلود تغییرات اعمال نشد | `npm run build:all` و Restart اپلیکیشن |

---

## 💻 اجرای محلی (برای توسعه و تست روی سیستم خودتان)

```bash
npm install            # یک‌بار
npm run build:all      # بیلد CSS + TypeScript
npm start              # یا: node dist/server.js
# سپس: http://localhost:3000
```

---

## 🧭 ساختار فنی کوتاه

- `src/server.ts` → وب‌سرور HTTP امن (بدون هیچ کتابخانه بیرونی)
- `src/router.ts` → مسیریابی، احراز هویت، CSRF، محدودیت نرخ، لاگ فعالیت
- `src/db.ts` → پایگاه‌داده JSON + داده اولیه (Seed)
- `src/ui.ts` → قالب صفحات، منو، فوتر، آیکون‌ها
- `src/public.ts` → صفحات عمومی سایت
- `src/admin.ts` → پنل مدیریت
- `src/util.ts` → توابع کمکی (تاریخ شمسی، امنیت، پاک‌سازی ورودی)

در صورت نیاز به ارتقا یا افزودن امکانات جدید (کارنامه، نمرات، پیامک و...)، همین ساختار قابل گسترش است.
